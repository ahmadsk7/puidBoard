# Realtime Server Environment Variables

# Server Configuration
PORT=3001
NODE_ENV=production

# CORS - Frontend URL (Vercel deployment URL)
CORS_ORIGINS=https://your-app.vercel.app,http://localhost:3000

# Redis (Upstash) - Optional, falls back to in-memory
REDIS_URL=

# Persistence Settings (optional)
SNAPSHOT_INTERVAL_MS=10000
SNAPSHOT_TTL_MS=3600000

# Storage (local for MVP, can upgrade to S3/Supabase later)
STORAGE_DIR=./.storage/tracks
CDN_BASE_URL=https://your-fly-app.fly.dev/files

# Database (Supabase - for future use if adding Postgres)
# DATABASE_URL=
